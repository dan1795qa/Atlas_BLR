# 🚀 Руководство по интеграции новых функций

Это руководство поможет вам интегрировать три новые функции в проект Atlas_BLR:

1. ✅ **Поиск по районам и городам**
2. ✅ **Фильтры на карте**
3. ✅ **Расширенные статистические данные**

---

## 📚 Шаг 1: Подключение новых скриптов

Добавьте следующие строки в файл `index.html` **перед закрывающим тегом `</body>`**:

```html
<!-- Новые модули -->
<script src="scripts/search.js"></script>
<script src="scripts/filters.js"></script>

<!-- Инициализация новых функций -->
<script>
    // Инициализация поиска
    let searchManager;
    
    // Инициализация фильтров
    let mapFilters;
    
    // Инициализируем после загрузки карты
    document.addEventListener('DOMContentLoaded', function() {
        // Ждем создания карты
        setTimeout(() => {
            // Инициализация поиска
            searchManager = new SearchManager(map);
            
            // Инициализация фильтров
            mapFilters = new MapFilters(map);
            
            console.log('✅ Поиск и фильтры инициализированы');
        }, 1000);
    });
</script>
```

---

## 🔍 Шаг 2: Интеграция поиска

Добавьте этот код в файл `app.js` **после загрузки GeoJSON**:

```javascript
// В функции fetch("belarus-regions.geojson")
.then((data) => {
    geojsonData = data;
    
    // ... существующий код ...
    
    // 🆕 ДОБАВЬТЕ ЭТО:
    // Загрузка локаций для поиска
    if (typeof searchManager !== 'undefined') {
        searchManager.loadLocations(data);
    }
    
    console.log("GeoJSON данные загружены успешно!");
})
.catch((error) => {
    console.error("Ошибка загрузки GeoJSON:", error);
});
```

Также добавьте в функцию `loadDistrictsByRegion`:

```javascript
function loadDistrictsByRegion(regionName) {
  const filePath = regionFiles[regionName];

  fetch(filePath)
    .then((response) => response.json())
    .then((data) => {
      currentDistrictData = data;
      
      // 🆕 ДОБАВЬТЕ ЭТО:
      // Добавляем районы в поиск
      if (typeof searchManager !== 'undefined') {
          searchManager.addDistricts(data.features, regionName);
      }
      
      showDistrictsList(regionName, data);
    })
    .catch((error) => {
      console.error("Ошибка загрузки районов:", error);
      alert("Не удалось загрузить районы");
    });
}
```

---

## 🎯 Шаг 3: Интеграция фильтров

Добавьте обработчик события фильтров в `app.js`:

```javascript
// Обработка изменения фильтров
window.addEventListener('filtersChanged', function(e) {
    const filters = e.detail;
    console.log('Фильтры изменены:', filters);
    
    // Перерисовка слоев карты
    if (geojsonData) {
        // Очищаем текущие слои
        regionsLayer.clearLayers();
        centersLayer.clearLayers();
        
        // Перерисовываем с учетом фильтров
        geojsonData.features.forEach(feature => {
            if (mapFilters.passesFilters(feature)) {
                const regionName = feature.properties.name;
                const props = feature.properties;
                
                if (feature.geometry.type === 'Polygon') {
                    // Добавляем области
                    const layer = regionPolygons[regionName];
                    if (layer) {
                        regionsLayer.addLayer(layer);
                    }
                } else if (feature.geometry.type === 'Point') {
                    // Добавляем города (используйте существующий код создания маркеров)
                }
            }
        });
    }
});
```

---

## 📊 Шаг 4: Использование статистических данных

Загрузите и используйте статистику в `app.js`:

```javascript
// Загрузка статистики
let statisticsData = null;

fetch('data/statistics.json')
    .then(response => response.json())
    .then(data => {
        statisticsData = data;
        console.log('✅ Статистические данные загружены');
    })
    .catch(error => {
        console.error('Ошибка загрузки статистики:', error);
    });
```

Добавьте в функцию `showRegionInfo`:

```javascript
function showRegionInfo(regionName, region) {
    // ... существующий код ...
    
    // 🆕 ДОБАВЬТЕ расширенную статистику:
    let extendedStats = '';
    if (statisticsData && statisticsData.regions[regionName]) {
        const stats = statisticsData.regions[regionName];
        extendedStats = `
            <div class="stats-card" style="margin-top: 16px;">
                <h4 style="color: var(--accent-color); margin-bottom: 10px;">📊 Расширенная статистика</h4>
                <div class="stat-item">
                    <span class="stat-label">🏛️ Районов:</span>
                    <span class="stat-value">${stats.districts}</span>
                </div>
                <div class="stat-item">
                    <span class="stat-label">🏙️ Городов:</span>
                    <span class="stat-value">${stats.cities}</span>
                </div>
                <div class="stat-item">
                    <span class="stat-label">💵 Ср. зарплата:</span>
                    <span class="stat-value">$${stats.averageSalary}</span>
                </div>
                <div class="stat-item">
                    <span class="stat-label">💼 Безработица:</span>
                    <span class="stat-value">${stats.unemployment}%</span>
                </div>
                <div class="stat-item">
                    <span class="stat-label">🌳 Лесистость:</span>
                    <span class="stat-value">${stats.forestCoverage}%</span>
                </div>
            </div>
        `;
    }
    
    // Добавьте extendedStats в infoPanelContent.innerHTML
    infoPanelContent.innerHTML = `
        ${existingContent}
        ${extendedStats}
    `;
}
```

---

## ⚙️ Функциональность

### 🔍 Поиск

- **Автозаполнение**: Начните вводить название города/района (мин. 2 символа)
- **Фильтрация**: Результаты сортируются по точности совпадения
- **Подсветка**: Выбранная локация подсвечивается на 3 секунды
- **Авто-зум**: Карта автоматически приближается к найденной локации

### 🎯 Фильтры

- **По типу**: Города, области, районы
- **По населению**: Диапазон в тысячах человек
- **По площади**: Диапазон в км²
- **Быстрые фильтры**: Крупные города, малые области

### 📊 Статистика

Доступные данные для каждой области:
- Население и площадь
- Плотность населения
- Количество районов и городов
- Уровень урбанизации
- Экономические показатели (GDP, зарплата, безработица)
- Основные отрасли промышленности
- Лесистость и сельскохозяйственные угодья

---

## 📦 Структура проекта

```
Atlas_BLR/
├── index.html              # Основной HTML файл
├── app.js                  # Основная логика карты
├── scripts/
│   ├── search.js          # ✅ НОВЫЙ: Модуль поиска
│   └── filters.js         # ✅ НОВЫЙ: Система фильтров
├── data/
│   └── statistics.json    # ✅ НОВЫЙ: Статистические данные
├── geojson/
│   ├── Минская область.geojson
│   ├── Брестская область.geojson
│   └── ...
└── belarus-regions.geojson
```

---

## 🚀 Запуск

1. Откройте `index.html` в браузере
2. Панель фильтров появится слева
3. Поиск доступен в верхней панели справа
4. Информация о регионах теперь включает расширенную статистику

---

## 🐞 Отладка

Если что-то не работает:

1. Откройте консоль разработчика (F12)
2. Проверьте сообщения:
   - `✅ Поиск и фильтры инициализированы`
   - `Загружено X локаций для поиска`
   - `✅ Статистические данные загружены`
3. Убедитесь, что все файлы загружены без ошибок

---

## 🎉 Готово!

Теперь ваш Atlas_BLR имеет:

- ✅ Мощный поиск с автозаполнением
- ✅ Гибкую систему фильтров
- ✅ Подробные статистические данные

Удачи в разработке! 🚀🗺️
